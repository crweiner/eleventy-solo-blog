---
layout: base.njk
title: Articles
description: All articles from the blog
permalink: /blog/
---

<section class="hero">
  <h1>Articles</h1>
  <p>Random thoughts. Randomly updated.</p>
</section>

{% if collections.posts %}
  <ul class="post-list">
    {% for post in collections.posts | reverse %}
      <li class="post-item">
        {% if post.data.featured_image %}
        <div class="post-image">
          <a href="{{ post.url }}">
            <img src="{{ post.data.featured_image }}" alt="{{ post.data.title }}">
          </a>
        </div>
        {% endif %}
        <div class="post-content-wrapper">
          <h2 class="post-title">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h2>
          <div class="post-meta">
            <time datetime="{{ post.date | w3DateFilter }}">{{ post.date | readableDate }}</time>
          </div>
          {% if post.data.excerpt %}
            <div class="post-excerpt">
              <p>{{ post.data.excerpt }}</p>
            </div>
          {% endif %}
          <a href="{{ post.url }}" class="read-more">Continue reading →</a>
        </div>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>No posts yet. Check back soon!</p>
{% endif %}
